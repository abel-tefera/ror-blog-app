<%= render 'shared/user_profile', user: @user %>

<div class="details">

    <div class="border_solid">
        <h1>Bio</h1>
        <p>
        <%= @user.bio %>
        </p>
    </div>
<% @user.three_recent_posts.each do |post| %>
    <div class="border_solid" >
        <h1>post <%= post.title %></h1>
        <p>
        <%= post.text %>
        </p>
        <p class="likes_box">Comments: <%= post.comments_counter %> Likes: <%= post.likes_counter %></p>
    </div>
<%end%>


<a href="/users/<%= @user.id%>/posts" class="user_all_posts">See all Posts</a>
</div>